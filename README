Libraries and programs required: 
Django 
Pillow
Jazzmin
Bootstrap 
Mysqlclient 
Html-css 
Mysql-Mysql Workbench
Programming languages:
Python 
Javascript

Instrucctions to running the program: 
Once you installed all the programs and libraries, you need to open mysql-workbench, create a new connection with the parameters that you want (make sure that this parameters must be the same at the settings.py file in the db connection) and then, open the sql file that you will find in this repository
Once you configured the mysql connection, you just need to open cmd or other console, place the file route in the project folder that you previously set, and execute the command: python manage.py runserver
That´s all you need to execute the program
